package Utilities;

//import org.testng.annotations.DataProvider;


public class excelDataProvider {
	/*
	 * //Main Directory of the project public static final String currentDir =
	 * System.getProperty("user.dir"); public static final String excelpath =
	 * currentDir + "/excel/TestData.xlsx"; public static final String
	 * excelSheetname = "Credentials";
	 * 
	 * 
	 * 
	 * @DataProvider(name = "test1data") public Object[][] getData() { Object
	 * data[][] = testdata(excelpath, excelSheetname); return data; }
	 * 
	 * public Object[][] testdata(String excelpath, String sheetName) { ExcelUtil
	 * excel = new ExcelUtil(excelpath, sheetName);
	 * 
	 * int rowCount = excel.getRowcount(); int colcount = excel.getColcount();
	 * Object data[][] = new Object[rowCount-1][colcount];
	 * 
	 * for(int i=1; i< rowCount; i++) { for(int j=0; j< colcount; j++) { String
	 * celldata = excel.getCellData(i, j); data[i-1][j] = celldata;
	 * 
	 * } System.out.println(); } return data;
	 * 
	 * }
	 */
}
